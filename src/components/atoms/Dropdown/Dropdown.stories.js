import React from 'react';
import Dropdown from './';

const story = {
  title: 'Atom/Dropdown',
  component: Dropdown,
};

export const Primary = () => <Dropdown>Login</Dropdown>;

export default story;
