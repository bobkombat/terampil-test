import React from 'react';
import PropTypes from 'prop-types';
import { Container } from './index.styled';

function About(props) {
  return <Container>{/* <img src={logoIcon} alt="logo" /> */}</Container>;
}

About.propTypes = {};

export default About;
